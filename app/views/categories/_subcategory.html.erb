<li>
<%= link_to h(subcategory.name), subcategory %> <%= "(#{subcategory.num_requests})" unless subcategory.num_requests==0 %>
<% if !subcategory.children.empty? %>
  <ul><%= render :partial => 'subcategory', :collection => subcategory.children %></ul>
<% end %>
</li>
