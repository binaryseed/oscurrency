<p>select multiple: cmd Mac / ctrl win</p>
<%=
    collection_select( model_name,
                      'category_ids',
                      @all_categories,
                      :id,
                      :long_name,
                      {},
                      {:multiple => true, :size => '10'}
                      )
%>
